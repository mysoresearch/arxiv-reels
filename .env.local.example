# Daily fetch cron secret — set this to protect /api/fetch-papers
CRON_SECRET=change-me-to-a-random-secret

# Vercel Postgres connection string (set automatically by Vercel Postgres integration)
POSTGRES_URL=postgres://user:password@host/dbname
